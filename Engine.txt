1. Organnigramme 		=> Miro
2. Versionning 			=> CMake
3. Architecture 		=> Actor/Component (UE)
4. Editeur 			=> custom
5. Garbage collector 		=> custom
6. Serialization 		=> custom
7. Interface engine (GUI) 	=> ImGui
8. Interface graphique 		=> OpenGl
9. Lib physique 		=> ReactPhysic3D
10. Editeur Reseau 		=> Enet
11. Editeur de sons 		=> Audeo

CMake: multiplatform, open src, module IDE existant
	-> CMakeList == .sln

  |-> path project + binaries a completer
  |-> CMakeLists (exe CMake) => cmake_minimum_required(VERSION 3.10) + project(NameProject) + add_executable(NameProject nameProject.c--)
	-> Configure -> Generate() => .sln dans binaries
   !! si regenerate: file/Delete cache -> configure -> finish
      NameProject == name folder


SMartPtr:
	SharedPtr => ‎count partage
	WeakPtr => Sécu si pointe nullptr















